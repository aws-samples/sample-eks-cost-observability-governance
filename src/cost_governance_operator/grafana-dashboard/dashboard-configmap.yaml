apiVersion: v1
kind: ConfigMap
metadata:
  name: cost-governance-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  cost-governance-dashboard.json: |-
    # Paste the contents of cost-governance-dashboard.json here
    # Or apply just the JSON file via Grafana UI import
