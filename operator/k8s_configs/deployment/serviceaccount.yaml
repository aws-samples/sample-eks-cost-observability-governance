apiVersion: v1
kind: ServiceAccount
metadata:
  name: cost-governance-operator
  namespace: cost-governance-system
  labels:
    app: cost-governance-operator
# EKS Pod Identity - no annotations needed
# Pod Identity association is created via AWS CLI (see Makefile)
